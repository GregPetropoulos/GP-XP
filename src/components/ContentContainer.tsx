

const ContentContainer = ({ children }: any) => {
  return (
    <div className='flex w-full justify-center px-2.5 sm:px-5 flex-wrap'>
      {children}
    </div>
  );
};

export default ContentContainer;
